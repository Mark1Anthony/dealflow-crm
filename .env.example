# Supabase project (Project Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Prepared demo account for the "Browse as demo user" button on /login.
# Read server-side only. Create this user in Supabase Auth first; if these
# are left empty the button reports that demo access is not configured.
DEMO_USER_EMAIL=
DEMO_USER_PASSWORD=
